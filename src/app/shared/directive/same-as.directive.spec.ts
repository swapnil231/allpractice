import { SameAsDirective } from './same-as.directive';

describe('SameAsDirective', () => {
  it('should create an instance', () => {
    const directive = new SameAsDirective();
    expect(directive).toBeTruthy();
  });
});
